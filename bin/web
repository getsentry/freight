#!/bin/bash -ex

PORT=${PORT:-9000}

if [ -n "$VIRTUAL_ENV" ]; then
    options="--virtualenv $VIRTUAL_ENV"
else
    options=""
fi

uwsgi \
    --http-socket :$PORT \
    --module ds.app \
    --callable app \
    --die-on-term \
    --master \
    --processes 4 \
    $options
