/** @jsx React.DOM */

var React = require('react');

var TaskDetails = React.createClass({
  render() {
    return (
      <div>
        Stuff
      </div>
    );
  }
});

module.exports = TaskDetails;
